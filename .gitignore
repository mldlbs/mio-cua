# Local model weights (OmniParser repo + ~1.3GB weights), download not committed
models/

# Python
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/

dist/

# Pre-publish content drafts (release schedule, channel drafts) - not public
publish/
